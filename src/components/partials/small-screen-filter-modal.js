import SidePane from "../side-pane";

const SmallScreenFilterModal = () => {
  return (
    <div id="small-screen-filter-modal" className="animate__animated animate__slideInDown animate__faster small-screen-filter-modal">
      <SidePane />
    </div>
  );
}

export default SmallScreenFilterModal;
